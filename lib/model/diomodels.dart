import 'package:flutter/material.dart';

class Codeopject {
  final String? url;
  final String? thumbnailUrl;
  final String? title;

  Codeopject(
      {required this.url,
      required this.thumbnailUrl,
      required this.title,
      required subtitle});
}
